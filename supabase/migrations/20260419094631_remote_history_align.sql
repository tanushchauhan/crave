-- Aligns with migration version already recorded on hosted Supabase (see `supabase migration list`).
-- No-op: schema changes from that deploy live on remote; this file exists so local `db push` can proceed.

select 1;
