/** sessionStorage key for handoff studio → explore (same tab). */
export const AD_CAMPAIGN_RESULT_KEY = "crave-ad-campaign-result";

/** Matches `infra/aws/lambdas/ad-generate/index.mjs`. */
export const AD_MAX_INPUT_IMAGES = 12;
export const AD_MAX_INPUT_BYTES = 14 * 1024 * 1024;
